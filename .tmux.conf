# Change the tmux default conig to make it more usable

## enable mouse
set -g mouse on

##  Key bindings ... 

# remap the prefix from 'C-b' to 'C-space'
unbind C-b
set -g prefix C-space
bind C-space send-prefix

# split panes using F2 with Ctrl or Shift
# unbind '"'
# unbind %
bind -n C-F2 split-window -h
bind -n S-F2 split-window -v

# switch panes using Alt-arrow without prefix
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# swap panes with Ctrl-arrow without prefix
bind -n C-Left swap-pane -s '{left-of}'
bind -n C-Right swap-pane -s '{right-of}'
bind -n C-Up swap-pane -U
bind -n C-Down swap-pane -D

# resize panes using Alt-Ctrl-arrow without prefix
bind -n M-C-Left resize-pane -L 5
bind -n M-C-Right resize-pane -R 5
bind -n M-C-Up resize-pane -U 5
bind -n M-C-Down resize-pane -D 5

## Design and appearance ...

set -g visual-activity off
set -g visual-bell off
set -g visual-silence off
setw -g monitor-activity off
set -g bell-action none
#  modes
setw -g clock-mode-colour colour5
# statusbar
set -g status-position bottom
set -g status-justify left
set -g status-left ''
set -g status-right-length 50
set -g status-left-length 20
